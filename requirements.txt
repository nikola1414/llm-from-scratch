# Core
torch>=2.0
numpy
# Notebooks
jupyter
ipykernel
matplotlib
# Data extraction (OpenWebText ships as .xz archives; Python's built-in `lzma`
# handles them. `pylzma` is optional and needs C build tools, see docs/00_setup.md)
tqdm
# Tests
pytest
